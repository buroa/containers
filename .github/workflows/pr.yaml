---
name: Release

on:
  pull_request:
    branches: ["master"]
    paths: ["apps/**"]
  push:
    branches: ["master"]
    paths: ["apps/**"]

concurrency:
  group: ${{ github.workflow }}-${{ github.event.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  id-token: write
  packages: write

jobs:
  changed-files:
    name: Get Changed Apps
    runs-on: ubuntu-latest
    outputs:
      apps: ${{ steps.changed-apps.outputs.all_changed_and_modified_files }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Determine changed apps
        uses: JJGadgets/tj-actions-changed-files@9200e69727eb73eb060652b19946b8a2fdfb654b
        id: changed-apps
        with:
          dir_names: true
          dir_names_max_depth: "1"
          matrix: true
          path: apps
          separator: ","

  build:
    needs: changed-files
    name: Build/Test ${{ matrix.app }} (${{ matrix.platform }})
    runs-on: ${{ matrix.platform == 'arm64' && 'ubuntu-24.04-arm' || 'ubuntu-latest' }}
    strategy:
      matrix:
        app: ${{ fromJSON(needs.changed-files.outputs.apps) }}
        platform: ["amd64", "arm64"]
      fail-fast: false
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Get Metadata
        id: metadata
        run: |-
          echo "version=$(yq '.version' apps/${{ matrix.app }}/metadata.yaml)" >> $GITHUB_ENV
          echo "test=$(yq '.test' apps/${{ matrix.app }}/metadata.yaml)" >> $GITHUB_ENV
          echo ${version}
          echo ${test}

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: "${{ github.actor }}"
          password: "${{ secrets.GITHUB_TOKEN }}"

      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Image
        uses: docker/build-push-action@v6
        id: build
        with:
          build-args: |-
            VENDOR=${{ github.actor }}
            VERSION=${{ steps.metadata.outputs.version }}
            REVISION=${{ github.sha }}
          outputs: |-
            type=docker
            type=image,name=ghcr.io/${{ github.actor }}/${{ matrix.app }},push-by-digest=true,name-canonical=true,push=${{ github.event_name != 'pull_request' }}
          context: ./apps/${{ matrix.app }}
          platforms: linux/${{ matrix.platform }}
          labels: |-
            org.opencontainers.image.title=${{ matrix.app }}
            org.opencontainers.image.url=https://ghcr.io/${{ github.actor }}/${{ matrix.app }}
            org.opencontainers.image.version=${{ steps.metadata.outputs.version }}
            org.opencontainers.image.revision=${{ github.sha }}
            org.opencontainers.image.vendor=${{ github.actor }}

      - name: Setup Goss
        if: ${{ steps.metadata.outputs.test }}
        uses: mlipscombe/goss-installation-action@v1.2.2
        with:
          version: latest

      - name: Run Goss Tests
        if: ${{ steps.metadata.outputs.test }}
        env:
          CONTAINER_RUNTIME: docker
          GOSS_FILE: ./apps/${{ matrix.app }}/ci/goss.yaml
          GOSS_OPTS: --retry-timeout 60s --sleep 2s --color --format documentation
          GOSS_SLEEP: 2
          GOSS_FILES_STRATEGY: cp
          CONTAINER_LOG_OUTPUT: goss_container_log_output
        run: |-
          dgoss run ${{ steps.build.outputs.imageid }}

      - name: Export Digest
        if: ${{ github.event_name != 'pull_request' }}
        id: digest
        run: |-
          digest="${{ steps.build.outputs.digest }}"
          digest="${digest#sha256:}"
          touch /tmp/${digest}
          echo "digest=${digest}" >> $GITHUB_OUTPUT

      - name: Upload Digest
        if: ${{ github.event_name != 'pull_request' }}
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.app }}-${{ matrix.platform }}
          path: /tmp/${{ steps.digest.outputs.digest }}
          if-no-files-found: error
          retention-days: 1

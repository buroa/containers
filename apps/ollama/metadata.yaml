---
app: ollama
semver: true
channels:
  - name: main
    platforms: ["linux/amd64", "linux/arm64"]
    stable: true
    tests:
      enabled: true
      type: web
  # There is no pre-release published currently
  # - name: beta
  #   platforms: ["linux/amd64", "linux/arm64"]
  #   stable: false
  #   tests:
  #     enabled: true
  #    type: web
